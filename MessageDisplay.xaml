<Window x:Class="OutOfOffice.MessageDisplay"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OutOfOffice"
        mc:Ignorable="d"
        Title="MessageDisplay" Height="540" Width="960" Loaded="Window_Loaded">
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="TextAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Name="rDefMain"  Height="9*"/>
            <RowDefinition Name="rDefSub" Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Name="txBlMain"/>
        <TextBlock Name="txBlSub"
                   Grid.Row="1"/>
    </Grid>
</Window>
